<!DOCTYPE html>
<html lang="es">
<head>
  <%- include('partials/_head') %>
</head>
<body class="min-h-screen p-6 bg-[var(--color-bg-light)] font-outfit">
    <%- include('partials/_header') %>

  <main class="max-w-7xl mx-auto">

    <section class="max-w-7xl mx-auto mb-8 p-4 bg-white rounded-xl shadow-lg border border-gray-100">
      <h2 class="text-xl font-light text-[var(--color-text-dark)] mb-4 border-b pb-2">Buscar y Filtrar Artículos</h2>

      <form method="get" action="/products" class="grid grid-cols-1 md:grid-cols-5 gap-4 items-end">
        <div class="md:col-span-2">
          <label for="nameFilter" class="block text-xs font-medium text-[var(--color-text-medium)] mb-1">Nombre</label>
          <input
            type="text"
            id="nameFilter"
            name="name"
            value="<%= (query && query.name) || '' %>"
            placeholder="Ej: Portátil"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm"
          />
        </div>

        <div>
          <label for="tagFilter" class="block text-xs font-medium text-[var(--color-text-medium)] mb-1">Etiqueta</label>
          <select id="tagFilter" name="tag" class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm">
            <option value="">-- Todos --</option>
            <option value="work" <%= (query && query.tag==='work'?'selected':'') %>>Work</option>
            <option value="lifestyle" <%= (query && query.tag==='lifestyle'?'selected':'') %>>Lifestyle</option>
            <option value="motor" <%= (query && query.tag==='motor'?'selected':'') %>>Motor</option>
            <option value="mobile" <%= (query && query.tag==='mobile'?'selected':'') %>>Mobile</option>
          </select>
        </div>

        <div class="md:col-span-2 grid grid-cols-2 gap-4">
          <div>
            <label for="minPrice" class="block text-xs font-medium text-[var(--color-text-medium)] mb-1">Precio Mín.</label>
            <input
              type="number"
              id="minPrice"
              name="priceMin"
              value="<%= (query && query.priceMin) || '' %>"
              min="0"
              class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm"
            />
          </div>
          <div>
            <label for="maxPrice" class="block text-xs font-medium text-[var(--color-text-medium)] mb-1">Precio Máx.</label>
            <input
              type="number"
              id="maxPrice"
              name="priceMax"
              value="<%= (query && query.priceMax) || '' %>"
              min="0"
              class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm"
            />
          </div>
        </div>

        <div class="md:col-span-5 flex justify-end">
          <button type="submit" class="py-2 px-6 bg-stone-700 text-white rounded-lg shadow-md hover:bg-stone-600 transition text-sm">
            Aplicar Filtros
          </button>
        </div>
      </form>
    </section>

    <section class="max-w-7xl mx-auto mb-6 flex justify-start items-center space-x-4">
      <span class="text-sm font-medium text-gray-600">Ordenar por precio:</span>
      <a href="/products?<%= new URLSearchParams(Object.assign({}, query || {}, { sort: 'price-asc' })).toString() %>"
         class="px-3 py-1 text-sm <%= (query && query.sort) === 'price-asc' ? 'bg-gray-200' : '' %> rounded-lg text-gray-700 hover:bg-gray-100 transition">
        Menor a Mayor
      </a>
      <a href="/products?<%= new URLSearchParams(Object.assign({}, query || {}, { sort: 'price-desc' })).toString() %>"
         class="px-3 py-1 text-sm <%= (query && query.sort) === 'price-desc' ? 'bg-gray-200' : '' %> rounded-lg text-gray-700 hover:bg-gray-100 transition">
        Mayor a Menor
      </a>
      <% if (query && query.sort) { %>
        <a href="/products?<%= new URLSearchParams(Object.assign({}, query || {}, { sort: '' })).toString() %>"
           class="px-3 py-1 text-sm text-red-500 hover:underline">
          Limpiar
        </a>
      <% } %>
    </section>

    <section class="max-w-7xl mx-auto">
      <h2 class="text-2xl font-light text-[var(--color-text-dark)] mb-6"><%= title %></h2>

      <div id="productList" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        <% (items || []).forEach(p => { %>
          <div class="product-card bg-white p-4 rounded-xl shadow-md hover:shadow-xl transition duration-300 border border-gray-100 flex flex-col">
            <div class="h-40 bg-gray-100 rounded-lg flex items-center justify-center mb-4 overflow-hidden">
              <i data-lucide="package" class="w-16 h-16 text-gray-400"></i>
            </div>

            <h3 class="text-lg font-medium text-[var(--color-text-dark)] truncate"><%= p.name %></h3>

            <p class="text-2xl font-bold text-green-700 mb-2"><%= p.price %> €</p>
            <div class="flex flex-wrap gap-2 mb-4">
              <% (p.tags || []).forEach(tag => { %>
                <span class="tag-style tag-<%= tag.toLowerCase() %>"><%= tag %></span>
              <% }) %>
            </div>

            <div class="mt-auto flex justify-between items-center pt-2 border-t border-gray-100">
              <span class="text-xs text-[var(--color-text-medium)]">Vendido por: <%= (p.owner && p.owner.name) ? p.owner.name : '—' %></span>
              <button class="delete-button text-red-500 hover:text-red-700 transition"
                      data-product-name="<%= p.name %>"
                      data-product-id="<%= p._id %>">
                <i data-lucide="trash-2" class="w-5 h-5"></i>
              </button>
            </div>
          </div>
        <% }) %>
      </div>

      <div class="flex justify-center items-center space-x-2 mt-8">
        <% for (let i = 1; i <= (totalPages || 1); i++) { %>
          <a href="/products?<%= new URLSearchParams({ ...query, page: i }).toString() %>"
             class="px-4 py-1 text-sm <%= i === (page || 1) ? 'bg-gray-200' : '' %> rounded-lg font-medium text-[var(--color-text-dark)]">
            <%= i %>
          </a>
        <% } %>
      </div>
  </section>

    <div id="deleteModal" class="hidden fixed inset-0 bg-black bg-opacity-30 flex items-center justify-center z-50">
      <div class="bg-white p-6 rounded-lg shadow-2xl w-full max-w-sm">
        <h3 class="text-lg font-medium text-red-600 mb-4 flex items-center space-x-2">
          <i data-lucide="alert-triangle" class="w-5 h-5"></i>
          <span>Confirmar Borrado</span>
        </h3>
        <p id="deleteMessage" class="text-sm text-[var(--color-text-medium)] mb-6">
          ¿Estás seguro de que quieres eliminar "Nombre Producto"? Esta acción es irreversible.
        </p>
        <div class="flex justify-end space-x-3">
          <button class="cancel-delete-button py-2 px-4 border border-gray-300 rounded-lg text-sm text-[var(--color-text-dark)] hover:bg-gray-100 transition">
            Cancelar
          </button>
          <button class="confirm-delete-button py-2 px-4 bg-red-600 text-white rounded-lg text-sm hover:bg-red-700 transition">
            Eliminar
          </button>
        </div>
      </div>
    </div>

    <script src="/javascripts/product-list-ui.js"></script>
    <script>lucide.replace()</script>

  </main>

</body>
</html>
