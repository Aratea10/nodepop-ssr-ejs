<!DOCTYPE html>
<html lang="es">
<head>
  <%- include('partials/_head') %>
  <link rel="stylesheet" href="/stylesheets/error.css" />
</head>
<body class="min-h-screen p-6 bg-[var(--color-bg-light)] font-outfit">
  <header class="max-w-7xl mx-auto flex items-center justify-between pb-6 border-b border-gray-200 mb-6">
    <h1 class="header-title league-script-regular text-[var(--color-text-dark)] font-bold">Nodepop</h1>
    <a href="/" class="text-sm text-[var(--color-text-medium)] hover:underline">Volver al inicio</a>
  </header>

  <main class="max-w-3xl mx-auto">
    <div class="error-container">
      <svg class="error-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
      <h2 class="error-title">Algo no ha ido bien</h2>
      <p class="error-message"><%= message || 'Error en el servidor' %></p>

      <% if (error && error.stack) { %>
        <details class="error-details">
          <summary>Mostrar detalles (solo en desarrollo)</summary>
          <pre><%= error.stack %></pre>
        </details>
      <% } %>

      <a href="/" class="error-back-link">Volver al inicio</a>
    </div>
  </main>

</body>
</html>
